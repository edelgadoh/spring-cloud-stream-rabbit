spring:
  rabbitmq:
    host: rabbitmq
    username: guest
    password: guest
    port: 5672

  cloud:
    stream:
      bindings:
        processorChannel:
          destination: processor-topic